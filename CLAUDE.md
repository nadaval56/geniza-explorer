# Geniza Explorer — הנחיות לקלוד

## חשוב: כמעט כל ה-HTML באתר נוצר אוטומטית

**אל תערוך `index.html`, `fragment.html` או קבצים תחת `d/`.** כולם נוצרים מחדש בכל בנייה
(גם ב-GitHub Actions בכל push ל-`main`), וכל שינוי ישיר בהם יידרס.

- לשינוי **הדף הראשי** — ערוך את תבנית `INDEX_HTML` בתוך `build.py`
- לשינוי **עמוד מסמך** — ערוך את תבנית `DOC_PAGE` בתוך `prerender.py`
- `about.html` הוא היחיד שנכתב ביד וניתן לעריכה ישירה

### מה נמצא איפה

| קובץ | תפקיד |
|------|--------|
| `build.py` | בונה את `index.html`, `fragment.html`, `data/search.json` ו-`data/docs/*.json` מ-CSV של Princeton Geniza Project. בסוף קורא ל-`prerender.py`. **גם גוזם** קובץ ב-`data/docs` שאינו ב-CSV עוד — ראו למטה |
| `prerender.py` | בונה עמוד HTML סטטי לכל מסמך תחת `d/`, מפתח סטטי, דפי הנושא תחת `t/`, `sitemap.xml` ו-`robots.txt` |
| `make_brand_assets.py` | מייצר את ה-favicon ואת `assets/og-image.png` מהגופנים והפלטה של האתר. הרץ רק אם משנים לוגו/צבעים |
| `index.html` | **נוצר אוטומטית** — אל תערוך |
| `fragment.html` | **נוצר אוטומטית** — דף הפניה בלבד (ראו למטה) |
| `d/` | **נוצר אוטומטית, gitignored** — ~36K עמודי מסמך |
| `t/` | **נוצר אוטומטית, gitignored** — 131 דפי נושא + מפתח הנושאים |
| `tag_pages.py` | התוכן של דפי הנושא: סלאג, כותרת, קבוצה ופסקת מבוא לכל תגית. `prerender.py` בונה דף **רק** לתגית שמופיעה כאן |
| `sitemap.xml`, `robots.txt` | **נוצרים אוטומטית, gitignored**. ה-`lastmod` מגיע מ-`data/lastmod.json` |
| `about.html` | נכתב ביד — ניתן לעריכה |
| `privacy/index.html` | מדיניות פרטיות — נכתב ביד, ניתן לעריכה |
| `accessibility/index.html` | הצהרת נגישות — נכתב ביד, ניתן לעריכה |
| `a11y_snippets.py` | שני בלוקי ה-HTML של הנגישות והפרטיות שכל דף נושא. `build.py` ו-`prerender.py` מייבאים ממנו |
| `CNAME` | הדומיין המותאם. `prerender.py` גוזר ממנו את כל הכתובות המוחלטות — **אל תמחק** |
| `googlef716f9558cc7d448.html` | אימות Google Search Console. תוכן מדויק שגוגל משווה — **אל תערוך ואל תמחק** |
| `assets/style.css` | עיצוב — ניתן לעריכה |
| `assets/search.js` | חיפוש, מפה, ענן תגיות — ניתן לעריכה |
| `assets/doc-image.js` | טעינת תצלום IIIF בעמודי `d/` — ניתן לעריכה |
| `assets/doc-english.js` | טוען את התיאור המקורי באנגלית מ-`data/docs/<id>.json` בלחיצה. הוא **לא** בתוך ה-HTML בכוונה — ראו ההערה בראש הקובץ |
| `assets/card-thumbs.js` | מרטיב את התצלומים בכרטיסי דפי הנושא, בגלילה. אותה לוגיקה כמו ב-`doc-image.js` |
| `data/tag_slugs.json` | **נוצר אוטומטית** על ידי `prerender.py` — תגית→סלאג, כדי ש-`search.js` יהפוך תווית בדף הבית לקישור |
| `assets/fonts.css` + `assets/fonts/` | גופנים מקומיים (SIL OFL). האתר לא פונה ל-Google Fonts |
| `assets/fragment.js` | **קוד מת.** הצופה הישן, לא נטען יותר על ידי אף עמוד |
| `assets/a11y.css` + `assets/a11y.js` | תפריט הנגישות ומצבי התצוגה — ניתן לעריכה |
| `assets/privacy.js` | הודעת הפרטיות, שער האחסון ופקדי הנתונים — ניתן לעריכה |
| `assets/legal.css` | עיצוב שני המסמכים המשפטיים בלבד |
| `assets/vendor/leaflet/` | Leaflet 1.9.4 מקומי. ראו ה-README שם לפני עדכון גרסה |
| `scripts/a11y-audit.mjs` | ביקורת נגישות אוטומטית. `node scripts/a11y-audit.mjs --root .` |
| `scripts/contrast.py` | מחשבון ניגודיות WCAG עם הצעת גוון חלופי |
| `data/tags_he.json` | תגיות לכל מסמך — נוצר על ידי `apply_tags.py` |
| `data/stats.json` | סטטיסטיקות — נוצר על ידי `build.py` |
| `data/lastmod.json` | **חייב להיות מחויב לגיט.** גיבוב תוכן ותאריך לכל עמוד, ומכאן ה-`lastmod` האמיתי ב-`sitemap.xml`. `prerender.py` מזיז תאריך רק כשהגיבוב זז. CI בונה אותו מחדש ולא דוחף בחזרה, ולכן אם לא יחויב — כל הכתובות ידווחו על תאריך היום, וזה האות שגוגל לומד להתעלם ממנו |
| `scripts/check_tag_pages.py` | מוודא שכל תגית עם 3+ מסמכים מקבלת דף, שהמבואות בטווח 80–200 מילים, ושה-`data/docs` מסונכרן. רץ ב-CI **לפני** הבנייה |

### מספר המסמכים — מקור אמת אחד

המספר נגזר תמיד מ-`data/docs/*.json`: `build.py` כותב אותו ל-`data/stats.json`,
ומשם הוא מגיע לכותרת, ל-meta description, ל-JSON-LD, לקוביית ה-KPI ולמפתח ב-`d/`.
אין לכתוב אותו ידנית באף תבנית.

פרינסטון מוחקת וממזגת PGPID מדי פעם. הקובץ המקומי של מסמך כזה שרד את המחיקה
ולא נגזם, ולכן שלושה יתומים — 5867, 18272 ו-26170 — נשארו ב-`data/docs` ונשאו
עדיין `"total": 35924` מבנייה ישנה. `prerender.py` בנה להם עמוד והכריז עליהם
ב-sitemap, ומכאן הפער: המפתח ב-`d/` ספר 35,940 בעוד דף הבית הצהיר על 35,937.

`build.py` גוזם עכשיו כל קובץ שאינו ב-CSV. הגיזום מדלג על `--limit`, ועוצר אם
נמצאו יותר מ-1% קבצים "מתים" — CSV קטוע לא ימחק את האוסף.

**בטקסטים שנכתבים ביד** (`README.md`, `about.html`, `accessibility/index.html`)
יש לכתוב "כ-36,000" ולא מספר מדויק: מספר מדויק בטקסט ידני מתיישן בבנייה הבאה
וסותר את הדף הראשי.

### כתובות של עמודי מסמך

כל מסמך יושב ב-`d/<pgpid>.html` — קובץ אמיתי ונפרד, עם כותרת, תיאור, canonical,
Open Graph ו-JSON-LD משלו בתוך ה-HTML. זה מה שמאפשר לזחלנים ולתצוגות מקדימות
(ווטסאפ, טלגרם) לראות תוכן בלי להריץ JavaScript.

`fragment.html?id=N` — הכתובת הישנה — עדיין עובדת, אבל היא רק מפנה ל-`d/N.html`
ומסומנת `noindex`. אל תקשר אליה בקוד חדש.

**כתובת בסיס:** `DEFAULT_BASE_URL` ב-`prerender.py`. אם מוסיפים דומיין מותאם, מספיק
ליצור קובץ `CNAME` — הסקריפט מזהה אותו לבד ומעדכן את כל הכתובות המוחלטות.

### Deploy

האתר עולה ל-GitHub Pages אוטומטית בכל push ל-`main` דרך `.github/workflows/deploy.yml`.
הזרימה: `build.py` → `prerender.py` → שלב אימות → פריסה ל-Pages.

`build.py` מסומן `continue-on-error`: אם ה-CSV של פרינסטון לא זמין, הבנייה ממשיכה
מהנתונים המקומיים ב-`data/docs/` במקום להפיל את הפריסה.

לבנייה מהירה בלי הורדת CSV: `python build.py --html-only`

### שני דברים שאסור לשבור בהגדרות הריפו

1. **ברירת המחדל חייבת להישאר `main`.** ריצות מתוזמנות רצות תמיד על ענף ברירת המחדל.
   כשברירת המחדל הייתה ענף ישן, ה-cron החודשי פרסם snapshot בן חודשיים מעל האתר החי
   שלוש פעמים ברצף. ה-checkout מוצמד ל-`ref: main` כהגנה נוספת.
2. **אל תחזיר את קבוצת ה-concurrency לשם `pages`.** היא נתקעה — ריצות נשארו ב-`queued`
   בלי jobs ובלי אפשרות ביטול. השם הנוכחי הוא `pages-deploy`.

### נגישות ופרטיות

שני המסמכים — `privacy/index.html` ו-`accessibility/index.html` — נכתבים ביד ואינם
נוצרים בבנייה. הם מתארים את מה שהאתר **באמת** עושה, ולכן כל שינוי שמוסיף שירות
חיצוני, כלי מדידה או מפתח אחסון חדש מחייב עדכון שלהם באותו commit.

- **תפריט הנגישות והודעת הפרטיות** נשתלים בכל דף דרך `a11y_snippets.py`. אל תוסיף
  אותם ידנית לתבנית חדשה — קרא ל-`a11y_snippets.head(root)` ו-`foot(root)`.
- **`privacy.js` נטען תמיד לפני `a11y.js`.** `a11y.js` שואל אותו אם מותר לכתוב
  לאחסון המקומי; בסדר הפוך השאלה נענית "כן" בשקט והבחירה של המשתמש נדרסת.
- **כל מידת טקסט חייבת להיות ב-`rem`.** פקד גודל הטקסט פועל בשינוי `font-size` של
  אלמנט השורש, ו-`px` מתעלם ממנו לחלוטין. חריג מכוון מסומן בהערה `a11y-allow-px`
  באותה שורה, והביקורת מדלגת עליו.
- **אל תוסיף CDN חיצוני.** הגופנים ו-Leaflet מוגשים מהאתר בדיוק מהסיבה הזו, ומדיניות
  הפרטיות מצהירה שאין קוד צד-שלישי.
- **לפני push שנוגע ב-CSS או בתבניות:** `node scripts/a11y-audit.mjs --root .`
  (דורש `npm i -D playwright`). בסביבה בלי גישה לרשת, בקשות אריחי המפה ומניפסטי
  ה-IIIF ייכשלו והביקורת תדווח על "שגיאות JS" ב-`/` וב-`/d/<id>.html` — זו הסביבה,
  לא האתר.

### תגיות: גבולות מילה ב-`tags_config.py`

לעברית אין `\b` שימושי. תחיליות (ו/ה/ב/ל/כ/מ/ש) וסופיות נדבקות לגזע, ולכן חיפוש
מחרוזת פשוט לא מבדיל בין `נרד` הבושם לבין `נרדף`. זה לא היה תיאורטי: התגית `נרד`
הייתה כמחציתה מסמכים על נרדפים, `משי` תפסה 628 הטיות של "ממשיך", `דיין` תפסה 299
של "עדיין", והדפוס `לחש` שמאחורי `מאגיה` תפס 156 מופעים של "לחשבונות".

**כל דפוס טקסט חדש חייב לעבור דרך `w()`**, שבונה ביטוי עם גבולות:

```python
"נרד": [w("נרד"), "שבולת נרד"]
"כבש": [w("כבש", suf="ים|ות|י|ה", pre="והבלכ")]
```

- `pre` — האותיות שמותר להן להקדים את הגזע, עד שתיים. **צמצם אותה** כשאות
  מהקבוצה יוצרת בדיוק את התפיסה השגויה: `ש` הוסרה מ־`חיטה` כי `ש`+`חיטה` היא
  "שחיטה", ו-`מ` הוסרה מ-`תמר` כי `מ`+`ש`+`תמר`+`ים` היא "משתמרים".
- `suf` — רשימת הסופיות המותרות במפורש. בלעדיה נתפס רק הגזע החשוף.

אחרי כל שינוי ב-`tags_config.py`: `python3 apply_tags.py`, ואז סנכרון שדה
`tags_he` ב-`data/docs/*.json` (או `build.py` מלא), ואז `scripts/check_tag_pages.py`.

### מפה וענן תגיות

- **הוספת מקום למפה**: הוסף רשומה ל-`MAP_LOCATIONS` ב-`assets/search.js`
- **הסתרת תגית מענן התגיות**: הוסף לסט `CLOUD_SKIP` ב-`assets/search.js`
- **הוספת דף נושא**: הוסף ערך ל-`TAG_PAGES` ב-`tag_pages.py`. פסקת המבוא חייבת להיות ייחודית ובאורך 80–200 מילים — תגית בלי מבוא לא מקבלת דף, וזה מכוון
- מקומות שנמצאים ב-`MAP_LOCATIONS` צריכים להיות גם ב-`CLOUD_SKIP` (כדי שלא יופיעו פעמיים)

## פרויקט ארוך-טווח: שכתוב תיאורים בעברית עם Opus 4.7

הקיימים ב-`data/translations_he.json` הם תקצירים קצרצרים שיוצרו על ידי Haiku בריצה הראשונית. אנחנו מחליפים אותם בהדרגה בתיאורים מלאים שנכתבים מחדש על ידי Opus 4.7 דרך Claude Code. נכון לכתיבת שורות אלה: כ-2,087 הוחלפו, כ-95 "מחפירים" עוד נשארו.

**מדידה מ-02/09/2026** (על `data/docs/*.json`, 35,937 מסמכים): לכל המסמכים יש תיאור
באנגלית, ול-35,931 מהם גם תיאור בעברית. אורך התיאור העברי החציוני הוא 117 תווים,
ו-**12,672 תיאורים קצרים מ-80 תווים** — אלה היעד האמיתי שנותר לריבריט. מאז שכל מסמך
מקבל עמוד משלו ב-`d/`, אורך התיאור העברי משפיע ישירות על איכות העמוד בעיני מנועי חיפוש.

**להמשיך את העבודה — השתמש בסקיל `/geniza-rewrite-batch`:**
- מוגדר ב-`.claude/skills/geniza-rewrite-batch/SKILL.md` (חלק מהריפו, זמין מכל צ'אט)
- שימוש: `/geniza-rewrite-batch [N]` — N batches × 40 ריבריטים ב-foreground, push ל-main בסוף כל batch
- הסקיל מכיל את כל הזרימה הנכונה (commit, push לסניף + main, fallback ל-merge, בדיקות זיהומים)

**קבצים רלוונטיים:**
- `rewrite_descriptions.py` — הסקריפט שעושה את העבודה (Opus 4.7 דרך `claude --print`)
- `find_translation_gaps.py` — מזהה את המסמכים עם הפער הגדול ביותר
- `.cache/rewrites_done.json` — מעקב אחרי מה שכבר נעשה (gitignored, אבל מתמשך על המכונה)

### חוק קשיח: פורמט `data/translations_he.json`

**תמיד** לכתוב את הקובץ עם `indent=2, sort_keys=True`. **לעולם לא** עם `separators=(",", ":")`.

הקובץ מכיל ~36K רשומות. בפורמט מצומצם הוא נכווץ לשורה אחת בגודל 9MB, וכל commit שמשנה רשומה אחת מציג ב-diff מחיקה של כל הקובץ + שורה חדשה אחת — בלתי ניתן לסקירה. הפורמט הקריא נותן שורה ל-ID, כך ששינוי של רשומה אחת = שורה אחת ב-diff.

זה תקף גם ב-`rewrite_descriptions.py` וגם ב-`translate.py`.
